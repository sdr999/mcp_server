# --- Core MCP tool server ---
fastmcp~=3.4
starlette~=0.35
uvicorn[standard]~=0.22
watchdog~=3.0
python-dotenv~=1.0

# --- Plugin runtime dependencies ---
httpx~=0.27.0
pyyaml~=6.0.1
aiosqlite~=0.20.0

# --- Optional / Web ---
fastapi~=0.109.0

# --- Optional Framework Wheels ---
# Install the local wheels only if your tools need them:
#   pip install lib/agentic_framework-1.0.0-py3-none-any.whl lib/agentic_adapters-1.0.0-py3-none-any.whl
