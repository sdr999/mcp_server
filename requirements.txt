# --- Core MCP tool server ---
fastmcp~=3.4
starlette~=0.35
uvicorn[standard]~=0.22
watchdog~=6.0
python-dotenv~=1.0

# --- Plugin runtime dependencies ---
httpx~=0.27.0
pyyaml~=6.0.1
aiosqlite~=0.20.0

# --- Optional / Web ---
fastapi~=0.109.0

# --- Phase 5: Optional Task Queue Backends ---
# Uncomment based on MCP_TASK_QUEUE_BACKEND setting:
# celery[redis]~=5.4       # MCP_TASK_QUEUE_BACKEND=celery
# arq~=0.26                # MCP_TASK_QUEUE_BACKEND=arq

# --- Optional Framework Wheels ---
# Install the local wheels only if your tools need them:
#   pip install lib/agentic_framework-1.0.0-py3-none-any.whl lib/agentic_adapters-1.0.0-py3-none-any.whl
