# --- Core MCP tool server ---
fastmcp~=3.4
# FastAPI is the top-level web framework; the FastMCP protocol app is mounted
# into it. FastMCP/sse-starlette require a modern Starlette (>=1.3), so FastAPI
# must be a recent release too (the old ~=0.109 pin forced Starlette <0.37 and
# was incompatible with FastMCP).
fastapi>=0.141,<1.0
starlette>=1.3
uvicorn[standard]~=0.51
watchdog~=3.0
python-dotenv~=1.0

# --- Plugin runtime dependencies ---
httpx~=0.27.0
pyyaml~=6.0.1
aiosqlite~=0.20.0

# --- Optional Framework Wheels ---
# Install the local wheels only if your tools need them:
#   pip install lib/agentic_framework-1.0.0-py3-none-any.whl lib/agentic_adapters-1.0.0-py3-none-any.whl
